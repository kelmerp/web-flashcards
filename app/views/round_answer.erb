<div class="container">
  <h1 class="deck_name"><%= @deck.name %></h1>
  
  <div class="flip"> 
    <div class="card"> 
      <div class="face front"> 
        <p><%= @card.question %></p>
      </div> 
      <div class="face back"> 
        <%= @card.answer %>
      </div> 
    </div> 
  </div> 

  <section class="result">
    <% if @card.guesses.last.status == "correct" %>
    <p><%= Guess.get_correct_response %></p>
    <% else %>
    <p><%= Guess.get_incorrect_response %></p>
    <% end %>
    <a id="next-card" href="/round/deck/<%= @deck.id %>/card/<%= @card.id %>/question">Next Card</a>
  </section>

  <section class="round_progress">
    <p><%= @number_correct %> / <%= @number_attempted %> correct</p>
  </section>
</div>
